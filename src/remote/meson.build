
with_driver_remote = get_option('with-driver-remote')
with_remote_daemon = get_option('with-remote-daemon')
if not with_remote_daemon or host_machine.system() == 'windows'
  with_remote_daemon = false
endif
